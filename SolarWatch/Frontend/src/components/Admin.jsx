import React from "react";

const Admin = ({ token }) => {
  return (
    <>
      <button
        className="user-settings-btn"
        onClick={() => {
          alert("Work in progress");
        }}
      >
        User settings
      </button>
    </>
  );
};

export default Admin;
